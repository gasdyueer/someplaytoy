import pyautogui as pg
import pyautogui
import pygetwindow as gw


from selenium import webdriver as wd

# 假设你已经设置好 WebDriver
# wd_options = webdriver.ChromeOptions()
# wd_options.add_argument("--start-maximized")
# driver = webdriver.Chrome(options=wd_options)
# driver.switch_to.window(driver.window_handles[0])  # 切换到浏览器窗口

# 执行 JavaScript 代码获取视口大小
# viewport_width = driver.execute_script("return window.innerWidth;")
# viewport_height = driver.execute_script("return window.innerHeight;")
#
# print(f"Viewport width: {viewport_width}, Viewport height: {viewport_height}")
# ok，得到了Viewport width: 1707, Viewport height: 773


